#pragma once

#include "pch.h"
#include "DeviceResources.h"
#include "Sun.h"

/*class SkyBox
{
public:
	SkyBox();
	~SkyBox() {};

	void Draw(DX::DeviceResources *resources, float time);

	bool CreateSkyBox(DX::DeviceResources *resources);
	bool BuildVerticesBuffer(DX::DeviceResources *resources, std::vector<Vertex::SkyBox> *vertices);
	bool BuildIndicesBuffer(DX::DeviceResources *resources, std::vector<UINT> *indices);

private:
	ID3D11Buffer *m_VB;
	ID3D11Buffer *m_IB;

	XMFLOAT4X4 m_mesh;

	Sun m_sun;
};*/

