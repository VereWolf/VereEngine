
#ifndef BILLBOARD_MESH
#define BILLBOARD_MESH

#include "pch.h"
#include "Vertex.h"
#include "Effects.h"
#include "Camera.h"
#include "LoadVMS.h"

class BillboardMesh
{
public:
	/*BillboardMesh();
	void Init(ID3D11Device* device,
		ID3D11DeviceContext* dc,
		Camera *camera,
		ID3D11Buffer *treesLODVB,
		XMDOUBLE2 position,
		XMINT2 coord);

	void Draw(ID3D11Device* device,
		ID3D11DeviceContext* dc,
		Camera *camera);

private:
	ID3D11Device *m_device;
	ID3D11DeviceContext* m_deviceContext;
	Camera *m_camera;

	ID3D11Buffer *m_treesLODVB;

	XMDOUBLE2 m_position;
	XMINT2 m_coord;

	XMFLOAT4X4 mMesh;

	XMFLOAT2 mTex[4];*/

	void PlaceHolder();
};

#endif //BILLBOARD_MESH